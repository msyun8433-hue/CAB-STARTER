# 이어가기 노트

> 기기(회사 PC ↔ 맥북)를 옮겨도 일이 끊기지 않게 하는 곳.
> 대화 기록은 안 따라오지만, **이 파일은 USB 타고 따라온다.**
> AI는 작업 시작할 때 이 파일을 **먼저** 읽고 이어간다.
>
> 쓰는 법 — 일 끝낼 때: "오늘 한 거 이어가기 파일에 정리해줘" / 시작할 때: "이어가기 읽고 시작하자"

---

## ⏭️ 다음에 할 일 (제일 위 = 제일 먼저)

### 🔥 [진행중] USB 졸업 → GitHub로 두 컴퓨터 잇기 (6/30)

**왜 이 얘기가 나왔나:** 윤미선이 "이 폴더를 구글드라이브로 옮기면 문제 생기나?"라고 물음. 결론 = **옮기지 마라. 이 폴더는 이미 GitHub에 연결돼 있다(태오가 CAB 수업에서 세팅).** 일반 문서 폴더는 드라이브 OK, 이 폴더만 GitHub. 이유 3가지 = ①비밀 키(.env·creds) ②git 작업 기록 ③태오가 만든 GitHub Actions 자동화 → 드라이브에 넣으면 비밀 새고 git 깨짐.
→ **개념 정리 문서 만들어 둠: `docs/USB-구글드라이브-깃허브_개념정리.md`** (윤미선이 나중에 천천히 읽고 이해하려고 함. 비유·표·단어풀이 포함.)

**확인된 현재 상태(6/30):**
- 이 폴더는 이미 GitHub 연결됨: `origin = https://github.com/msyun8433-hue/CAB-STARTER.git` (윤미선 계정).
- 브랜치 `main`, **origin보다 1 커밋 앞섬(ahead 1)** + 안 올린 수정분 2개: `이어가기.md`, `하루끝-키트/사이트_레벨2/api/mirror.js`.
- 즉 **이 PC 작업이 아직 GitHub에 안 올라가 있음.** 이대로 맥북서 받으면 최신 안 옴.
- 폴더 위치는 아직 USB(F:). 그래서 지금은 USB를 들고 다녀야 하는 상태.

**다음에 할 일 (순서대로 — 윤미선 OK 받고 진행):**
1. ✅ **[완료 6/30] 지금 작업 GitHub에 올리기(push).** 커밋 `9893074` 로 push 완료. 올리기 전 **비밀/쓰레기 파일 정리**까지 함(아래 ⚠️ 참조). origin/main = 최신.
2. ⏳ **회사 PC 자기 하드(C: 등)에 한 번 내려받기(clone).** 그러면 이 PC는 USB 없이도 작동. ※주의: 비밀 파일(.env·creds·gws 열쇠 등)은 .gitignore라 GitHub에 없음 → clone 후 비밀은 PC마다 다시 넣어야(노션·supabase·vercel 인증과 같은 원리, 기존 이어가기 기록 참조).
3. ⏳ **맥북에서도 한 번 내려받기(clone).** "이어가기 읽고 GitHub에서 내려받자" 하면 됨.
4. 그 뒤부터 **USB 없이** pull(받기)/push(올리기)로만 두 컴퓨터 이어짐. AI가 매 세션 대신 눌러줌. USB는 여분 백업으로만 보관.

**✅ 6/30 정리한 것:** 무서운 이름의 `creds.yml`·`api-lib.yml`·`keyform.yml`·`kf2.yml`·`create-proj.yml` 은 **진짜 비밀이 아니라 Playwright(브라우저 자동조작)가 찍은 웹페이지 화면 덤프**였음 → 추적 해제 + .gitignore 추가. `.playwright-mcp/` 로그·스샷도 정리. **앞으로는 안 올라감.**

**‼️ 아직 남은 보안 1건(중요·윤미선 차례):** `gws-backup/token_cache.json`·`credentials.enc`(구글 워크스페이스 OAuth 토큰)이 **옛 커밋(초기 커밋)에 이미 올라가 있어** 지금 추적만 끊었을 뿐 **GitHub 이력엔 아직 남아 있음.** 비공개 repo라 위험은 제한적이지만 깔끔히 하려면 → **① gws 재인증(`gws auth login`)으로 옛 토큰 무효화**(이게 제일 확실·간단) ② 필요하면 BFG로 이력에서 제거. 결제·DM 아니고 토큰 갱신이라 윤미선이 한 번 눌러주면 됨.

---

### 🔥 [진행중] 이어거울 사이트 — 관리자/나의페이지/저장 전부 작동 (6/29)

**오늘 고친 것 (Opus, 실제 API로 검증 완료):**
- ✅ **거울글 2줄 → 458자 완전한 글.** 원인=Gemini 2.5 Flash는 thinking 모델이라 `maxOutputTokens:1024`를 추론이 다 먹어 출력이 잘림 → 8192로 늘림 (`api/mirror.js`).
- ✅ **거울글 저장(mirrors 테이블).** 원인=`mirrors.session_id`(uuid FK)에 `session-<timestamp>` 문자열을 넣어 FK 위반 → session insert 후 `.select('id')`로 진짜 uuid 받아 연결 (`api/mirror.js`).
- ✅ **관리자/나의페이지 500.** 원인=실제 `sessions` 테이블엔 `user_name`·`user_contact` 컬럼이 **없는데** API가 그걸 조회 → 제거 (`api/mirrors.js`·`api/sessions.js`·`admin-functions.js`·`mypage-functions.js`).
- ✅ **구글 로그인.** 기존 클라이언트ID는 '데스크톱' 타입이라 웹 로그인 불가 → **웹 애플리케이션** OAuth 클라이언트 새로 생성. ID=`71496735255-mr285j4cas006tli8di6lqm0e6p5d765.apps.googleusercontent.com`. 승인된 JS 출처/리디렉션=`https://harukkeut-site.vercel.app`. admin.html·mypage.html에 `google.accounts.id.initialize+renderButton` 추가.
- ✅ **상단 탭** (기록하기/관리자/나의 페이지) 3개 HTML에 추가.

**Supabase 실제 스키마 (ptluntkhhszuircqztjr, 추측 금지·이대로):**
- `sessions`: id(uuid), user_id, recorded_at, sensations, one_word, scene, scene_sensation, scene_interpretation, voice_a, voice_b, one_sentence, sentence_feeling, tomorrow_action, gratitude(jsonb), created_at, updated_at — **연락처/이름 컬럼 없음**
- `mirrors`: id(uuid), session_id(uuid FK→sessions.id), mirror_text, metadata(jsonb), created_at
- ⚠️ sessions·mirrors·visits **RLS 꺼짐** = anon키로 누구나 접근 가능. 출시 검증 끝나면 잠글 것.

**추가로 고친 것 (6/29 오후, 실제 API 검증 완료):**
- ✅ **이름 반영.** 폼 `이름` 칸을 거울글 생성에 전달 → "○○님" 대신 **"윤미선님,"** 으로 시작. sessions에 `user_name`·`user_contact` 컬럼 추가(마이그레이션 `add_user_name_contact_to_sessions`)하고 저장. 관리자/나의페이지에 이름·연락처 표시 → 누가 보냈는지 구분 가능.
- ✅ **중복 저장 막음.** 원인 2개: ① submit 핸들러가 session.html·mirror-functions.js **두 곳에 중복 등록**돼 한 번 눌러도 2건 저장 → session.html 쪽 제거. ② 피드백이 없어 사용자가 계속 누름 → `isSubmitting` 가드 + 보내기 버튼(`#sendBtn`) 잠금 + **"거울을 비추는 중…"** 표시 추가.

**추가로 고친 것 (6/29 저녁 — ✅ 배포까지 완료):**
- ✅ **고객한테 옛 입력이 남아있던 문제.** 원인=session.html 자동저장(`haruEnd_기본_v1`)이 "보내기" 뒤에도 안 지워져 다음에 열면 지난 글이 복원됨 → `displayMirror`(`mirror-functions.js:217-226`)에 `localStorage.removeItem` + `form.reset()` 추가. (작성 중 보존 기능은 그대로 유지)
- ✅ **고객한테 '초안 생성/프롬프트' 개발자 버튼이 보이던 문제.** 원인=`session.html#draft-prompt`(claude -p 손수 붙여넣던 옛 방식)가 노출 → `display:none` 처리. 고객은 "보내기"만 봄.
- ✅ **배포 완료(6/29).** 회사 PC에 Vercel CLI 로그인을 처음으로 마침(OAuth device flow — `vercel login` → 브라우저 승인). 이제 회사 PC에서도 AI가 `vercel --prod`로 자동 배포 가능. 위 두 수정 라이브 반영·검증 완료(`harukkeut-site.vercel.app` 에서 `draft-prompt style=display:none`·`removeItem('haruEnd_기본_v1')`+`form.reset()` 확인). ⚠️ Vercel 인증도 노션·supabase처럼 **PC마다 한 번씩** 필요(비밀은 USB 안 담김) — 다음에 또 막히면 `vercel login` 한 번.
- ⚠️ **테스트 인프라 깨짐(내 변경과 무관, 변경 전에도 동일):** jest가 ESM/CommonJS 충돌로 실행 불가(`session.test.js`는 require, 프로젝트는 ESM). 복구는 별도 작업.

**추가로 고친 것 (6/29 밤 — 거울글 "됐다 안됐다" 신뢰성):**
- ✅ **재시도+여러 모델 폴백 배포.** `/api/mirror`가 Gemini 한 번 실패에 곧장 500을 뱉던 걸 → 모델 5개(`2.5-flash`×2→`2.0-flash`→`2.5-flash-lite`→`1.5-flash`)를 순서대로 시도, 하나 막히면 다음으로. `maxDuration:60`. 무서운 "서버 오류 500" → 담백한 안내("지금은 잠시 붐비고 있어요…")로 교체(프론트+백엔드, [결다듬기] 통과). 검증: 같은 요청 7/7 성공(이전 3/5).
- ‼️ **진짜 큰 원인 발견 = 무료 등급 일일 할당량.** Gemini API 키가 **무료 등급(free tier)** 이라 `gemini-2.5-flash` 하루 **20회/모델** 제한. 20회 넘으면 429(quota exceeded)로 그날은 막힘. = 신뢰성의 핵심 구멍. 모델 폴백으로 하루 ~100회(5모델×20)까지 늘었음. **결제(billing) 연결하면 한도 대폭↑·429 사라짐**이지만 → **[결정 6/29] 윤미선: 결제 안 하고 무료로 간다.** 베타는 ~100회/일로 충분. ※ 6/29 내 테스트로 2.5-flash 오늘 할당량 소진됨(내일 리셋).
- ⏳ **[내일 할 일] 거울글 품질(틀) 개선.** 현재 프롬프트는 칸을 하나하나 받아써서 기계적("내 해석으로 이어졌습니다", "한 단어로 요약되며" = 설문 읽어주는 말투 → AI 티). "편지처럼 / 칸 이름 넣지 말고 / 마음 담긴 말만 골라 자연스럽게 엮기" 새 프롬프트 **초안 작성함**(아래 보관, `api/mirror.js`엔 아직 미적용). 좋은 모델(2.5-flash) 할당량이 차서 깔끔한 before/after 데모를 못 봄 → **내일 할당량 리셋되면: ① 새 프롬프트로 2.5-flash before/after 뽑아 윤미선에게 보여줌 → ② 윤미선이 결 확정 → ③ `api/mirror.js` 프롬프트 교체 후 배포.** 거울글은 §0 핵심이라 최종 결은 윤미선이 고름.
- ⚠️ **임시파일 `사이트_레벨2/_tmp_compare.mjs` 남음**(비교 스크립트, 비밀 없음). rm 막혀서 못 지움 — 다음에 삭제(rm 허용해주면 바로). 배포돼도 무해(라우트 아님)하나 정리 권장.

**[보관] 내일 적용 검토할 "편지형" 거울글 프롬프트 초안:**
```
당신은 거울입니다. 한 사람이 오늘 하루를 적어 보냈습니다. 당신이 할 일은 그 사람의 말을
조용히 정돈해, 짧은 편지처럼 돌려주는 것입니다. 새로운 해석도, 조언도, 위로의 말도 보태지
않습니다. 그저 그 사람이 쓴 것을, 그 사람이 다시 읽을 수 있게 비춰줍니다.

[그 사람이 적은 것]
- 몸의 느낌 / 오늘을 한 단어로 / 떠오른 장면 / 그때 몸 / 그때 든 생각 /
  한쪽 목소리 / 다른쪽 목소리 / 오늘의 한 문장 / 그 문장을 읽을 때 /
  내일 해보고 싶은 것 / 고마웠던 것  (← 실제 값 채워서 전달)

[쓰는 법]
- "○○님,"으로 시작하는 짧은 편지. 세네 문단, 여섯~열 문장.
- 항목을 하나하나 받아쓰지 말 것. 칸 이름("몸의 느낌은","한 단어는","내 해석은")을 문장에 넣지 말 것.
  마음 담긴 말만 골라 편지 쓰듯 엮기. 안 맞는 항목은 빼도 됨.
- 그 사람의 단어를 살리되 기계적 연결("~이고,~이며,~있으며,~입니다") 반복 금지. 문장 길이 다르게.
- 두 목소리는 둘 다 그 사람 안에 있다고 가만히 비춤. 어느 쪽 편들지 말 것.
- 단 한 번, 반복되는 결 하나를 괄호 안 기울임으로 (해석 아니라 메아리처럼). 없으면 만들지 말 것.
- 마지막은 닫지 말고 여백으로. 다짐·약속 시키지 말 것.
- 차분·담백. 시처럼 꾸미거나 감상에 젖지 말 것. 느낌표·이모지 금지.

[절대 쓰지 않는 말] 치유·회복·성장·변화·자기계발·코칭·클래스·교육·강의·명상·소마틱·바디스캔·
이완·마음챙김·호흡법, "당신도 할 수 있어요"·"내일은 나아질 거예요" 같은 위로·다짐·약속.

편지만 출력. 맨 끝 줄에 — 이어 붙이기.
```

**다음 할 일:**
1. ✅ **[완료 6/29] 기존 중복 데이터 정리** — 윤미선 확인 후 중복 6건 삭제(그룹별 1건만 남김). 세션 10→4, 거울글 6→3(CASCADE 자동). 남은 4건: 자고싶다·버거움(끝까지 버텼다)·무기력함·윤미선(버거움/오늘도 버텼다=이름연락처 있는 진짜 테스트).
2. ⏳ **[핵심·윤미선 차례] 이메일 발송 — Gmail 앱비번으로 결정.** 코드는 이미 완성(수정 불필요), nodemailer도 설치돼 있음. **남은 건 Vercel 환경변수 6개뿐** → 비밀(앱비번)이라 AI가 못 넣음. **절차서: `하루끝-키트/사이트_레벨2/이메일_Gmail_연결.md`** (앱비번 발급 → SMTP_HOST/PORT/SECURE/USER/PASS/FROM 6개 입력 → 재배포). 윤미선이 한 번만 따라 하면 켜짐. SMTP_PASS만 비밀, 나머지 5개 값은 절차서에 그대로 적어둠.
3. 보안: RLS 잠그기 (위 경고). — 출시 검증 끝나면.

---

### 🔥 CAB 3주차: 대시보드 자동화 설계 — 마인드맵 작성 완료 (6/28)

**마인드맵:** `business/04-자동화/CAB-3주차-대시보드자동화.opml`

**목표:** 세션 현황판(하루끝 대시보드)의 ①~⑦ 전체 파이프라인을 자동화
- ① 상태 입력 (폼)
- ② 재료 모으기 (Claude)
- ③ 질문지·과제 (Claude)
- ④ 대본 초안 (Claude)
- ⑤ AI티 빼기 (humanize-korean)
- ⑥ 결 점검 (자동 검증 + 윤미선 수동 승인)
- ⑦ 녹음·발행 (아직 수동)

**1차 MVP:** 입력 폼 + ②~⑤ 자동 생성 + ⑥ 승인 버튼 → 현황판 자동 갱신

---

### [DONE] 이어거울 실시간 생성 구현 — 완전히 완료 (6/24 밤)

- [x] **Task 2: /api/mirror 엔드포인트 — 완료(6/24)** ✅
  - Anthropic Claude → Google Gemini API로 마이그레이션
  - 환경변수 검증, 빈 응답 검증 추가
  - 테스트: 5/5 passing
  - 커밋: `29506d0` feat: Migrate mirror API from Anthropic to Google Gemini

- [x] **Task 3: Supabase 테이블 & 배포 — 완료(6/24)** ✅
  - sessions, mirrors, visits 테이블 생성 (ptluntkhhszuircqztjr)
  - **배포 완료:** https://harukkeut-site.vercel.app
  - 라이브 테스트 준비 완료

- [ ] **라이브 테스트 (윤미선 & 첫 사용자)** — 예정(6/24~25)
  - 웹사이트 열기: https://harukkeut-site.vercel.app/session.html
  - 폼 작성 → "보내기" → 거울 글 바로 표시 ✨
  - 이메일 도착 확인 (msyun8433@gmail.com)
  - Supabase 저장 데이터 확인
  - 거울 글 톤 검증 (AI 티 없는지, 금지어 없는지)
  - **3가지 질문에 답변:**
    ① 이 편지가 나에게 도착하나 / 차갑나
    ② 괄호 살짝비춤이 딱 좋은 깃털인가 / 해석으로 반발 넘었나
    ③ 내일칸 비워둔 마무리가 거울다운가 / 허전한가
  - 피드백 → AI가 prompt 조정

- [ ] **Task 4: 타임라인 조회 페이지** — 예정 (Task 3 완료 후)

- [ ] **(이어서) 워크북 PDF 다시 인쇄 + 사이트 재배포** — 베타 피드백 반영 작업 중 *마지막 두 단계만 남음.* ①**workbook.pdf 재생성**: HTML 원본(`하루끝-키트/outputs/워크북_하루끝-나에게_v0.1.html`)은 이미 "지금, 몸의 감각"으로 고쳐둠 → **인쇄만 하면 됨**. (회사 PC에서 Edge `--headless=new --print-to-pdf` 시도했으나 exit 2로 실패 — 한글경로 피해 `C:/temp_wb/wb.html`로 복사 후 시도했는데도 안 됨. 다음에 재시도하거나, **브라우저에서 그 HTML 열고 인쇄→PDF로 저장**해 `하루끝-키트/사이트_레벨2/downloads/workbook.pdf` 교체. Word는 이미 반영 끝남.) ②그다음 **`cd 하루끝-키트/사이트_레벨2 && vercel --prod`** 로 배포(§10 AI 가능). ③폰에서 줄바꿈·게임 비춤문장 시간 눈으로 확인.

- [ ] **(이어서) "보내기" → supabase 저장 연결** — supabase MCP **인증 완료됨**(아래). `session.html` "보내기"를 supabase 테이블에 저장하게 연결하는 일이 아직 남음(지금은 FormSubmit 메일만). 프로젝트 ref `ptluntkhhszuircqztjr`(msyun8433-hue's Project, 서울). 이게 §0(몸 안 갈리는 구조)에 가장 맞는 빈 구멍.

- [ ] **윤미선: "보내기" → 구글시트 저장 연결** — 절차 `하루끝-키트/사이트_레벨2/보내기_구글시트_연결.md` (sheets.new → 앱스스크립트 붙여넣기 → 웹앱 배포 → URL). 지금은 "보내기" 눌러도 저장 안 됨(ENDPOINT 비어 있음). URL 주면 AI가 session.html에 넣고 재배포.

- [ ] **윤미선: 첫 세션 v0.3(10분판) 녹음** — `outputs/오디오대본_하루끝-나에게_v0.3_10분.md`. 현재 사이트 오디오는 13분 임시. 녹음 주면 AI가 `audio/session.m4a` 교체 후 재배포(`vercel --prod`).

- [ ] **윤미선: 메인 컨셉 정하기** — "하루 10분"은 홍보 문구일 뿐 메인 아님. 더 좋은 메인 컨셉 있다고 함 → 정하면 반영. (미정)

- [ ] **윤미선: 영어명 단어 최종** — 브랜드 영어명 **Trace** 잠정 확정(흔적·이어지는 선). 프로젝트/주소는 아직 `harukkeut-site` → 나중에 trace로 변경 or 진짜 도메인.

- [ ] **윤미선: 씨앗 파일 검토** — `하루끝-키트/seeds/yunmiseon_seed.md` 읽고 틀린 결 고치기

- [ ] **v2 Day 3:** 상태 입력(inputs/state_distance.md) + 출력 틀(templates/session_template.md) 만들기

- [x] **폴더 이름 바꾸기** — `cab-starter` → **`이어(CAB-STARTER)`** 완료(6/21). 안의 내용 그대로 따라옴. (VS Code는 새 경로로 다시 열어야 함)

- [ ] **윤미선/AI: 회사 PC 설치 마저 — 시간 날 때** — 맥북엔 다 깔렸고 회사 PC엔 **GWS CLI**·**UI/UX Pro Max** 2개만 없음(+Skill Creator·humanizer 종류 확인). 정확한 상태·설치법은 `docs/회사PC-설치체크리스트.md`. **앤트로픽 공식 문서 기반 설치.** 급하지 않음.

- [ ] GitHub 로그인 준비 (다음 주까지)

---

## 📍 지금 어디까지 했나

### 🔥 [진행 중] 이어거울 — Hugging Face API 설정 중 (6/28 회사 가기 전)

**현황:**
- Claude API: ❌ 신용 부족 (Anthropic 결제 시스템 한국 결제 미지원)
- Google Gemini: ❌ 권한 없음 (API 키로 모델 접근 불가)
- **Hugging Face: ⏳ 토큰 발급 대기 중**

**했던 것:**
1. ✅ mirror.js 코드 수정 (Gemini → Hugging Face로 변경)
2. ✅ package.json 업데이트 (`@google/generative-ai` 다시 추가)
3. ✅ Vercel 배포 완료
4. ⏳ Hugging Face 토큰 발급 대기 (사용자가 회원가입 및 토큰 복사 중)

**다음 단계:**
1. Hugging Face 토큰(`hf_...`) 받으면 → Vercel 환경변수 설정
2. 배포 → 라이브 테스트
3. mirror.js 즉시 작동 여부 확인

**배포 URL:** https://harukkeut-site.vercel.app
**코드:** `/하루끝-키트/사이트_레벨2/api/mirror.js`

---

### 대시보드 자동화 — Task 1: Supabase 스키마 확장 ✅ (6/28)
**sessions_queue · session_scripts 마이그레이션 작성 완료**

#### Task 1: Supabase 스키마 확장 ✅
- sessions_queue (AI 생성 세션 큐), session_scripts (파이프라인 단계별 결과물) 테이블 설계
- 마이그레이션: `migrations/004_sessions_queue.sql`
- 검토 항목:
  - sessions_queue (10개 컬럼): id, status(enum), state_name, materials_text, created_at, updated_at, generation_started_at, generation_completed_at, approved_at, approver_notes, storage_path
  - session_scripts (9개 컬럼): id, session_id(FK CASCADE), stage(int), stage_name, content, version, status(enum), created_at, updated_at
  - RLS: authenticated 사용자만 SELECT 가능 (INSERT/UPDATE/DELETE는 service_role/backend만)
  - 제약조건: CHECK(status IN (...)), UNIQUE(session_id, stage)
  - 트리거: session_scripts.updated_at 자동 갱신
  - 인덱스: 워커 패턴용 부분 인덱스, 복합 인덱스 최적화

### 이어거울 Task 1-3 완료 (6/24 밤)
**subagent-driven-development로 구현 완료 → 라이브 테스트 대기**

#### Task 1: Supabase 스키마 설계 ✅ (이전)
- sessions (고객 기록), mirrors (거울 글), visits (재방문) 테이블 설계
- 커밋: 78b7c82

#### Task 2: `/api/mirror` 엔드포인트 ✅
- TDD로 mirror.js 구현 (326줄, Claude API + Supabase + Email)
- 테스트: 10/10 pass (mirror.test.js 274줄)
- code-reviewer agent → HIGH 4개 이슈 발견 & 수정 (모델ID, 입력검증, 메일헤더, 오류핸들링)
- 커밋: 94d5086

#### Task 3: session.html 프론트엔드 통합 ✅
- mirror-functions.js (185줄) — collectFormData, displayMirror, submitAndShowMirror, escapeHtml
- 테스트: 19/19 pass (session.test.js 328줄)
- session.html 수정: 거울 표시 섹션 추가, 폼 제출 이벤트 연결, localStorage 저장
- 커밋: 5f9f1b4

#### 기술 스택
- Frontend: session.html + mirror-functions.js (vanilla JS)
- Backend: Node.js Express API (mirror.js)
- Database: Supabase PostgreSQL (sessions, mirrors, visits)
- AI: Hugging Face (거울 글 생성 — Claude API 신용 부족, Gemini 권한 없음)
- Email: FormSubmit.co (비동기 이메일)

**현재:** Hugging Face 토큰 설정 대기
**다음:** 토큰 → Vercel 환경변수 → 배포 → 라이브 테스트

---

## 📋 과거 작업 기록 (시간 역순)

### 기록 너무 길어 최근만 유지 (6/21 이후)

- **베타 피드백 반영 작업(6/22 · 회사 PC, 퇴근으로 중단):** 첫 베타 사용자 후기 받음 — "엄청 잘 만들어졌다, 이어와 **91%** 나왔다"(호평). 개선 요청 3개를 작업: ①**폰 줄 띄어쓰기** → `quiz.html`·`session.html` body에 `word-break:keep-all; overflow-wrap:break-word;` 추가(한국어가 폰에서 단어 단위로 깔끔히 줄바꿈). ✅완료 ②**어울림 게임 글씨 빨리 사라짐**(답 고르면 비춤 한 줄이 1.4초 뒤 넘어가 못 읽음) → `quiz.html` setTimeout 1400→**2800ms**. ✅완료 ③**설문 후 안내문 AI 티** → **[결다듬기] 스킬**로 거름(금지어·과장 0 확인). `session.html` 3곳 수정: ⓐ워크북 안내 반복문장 정리 ⓑ"몸의 감각 체크인"→**"지금, 몸의 감각"**(외래어 '체크인' 뺌) ⓒ"보내지 않아도 괜찮아요"→"그대로 두어도 돼요"(~괜찮아요 3연속 변주). 윤미선이 "1·2·3 다 반영 + 워크북도 같이"로 선택. **워크북도 동기화:** HTML 원본 2개(`outputs/워크북_하루끝-나에게_v0.1.html`·`워크북_타이핑용_*.html`)와 **Word(`downloads/workbook-typing.docx`, python-docx로)** 까지 "지금, 몸의 감각" 반영 ✅. ⚠️**남은 것: PDF(workbook.pdf) 미반영 + 사이트 미배포 + 보내기 supabase 미연결** → 위 '다음에 할 일' 2개 참조. (변경 파일은 다 저장됨, 배포만 안 함 → 라이브 사이트엔 아직 옛 버전.)

- **supabase MCP 인증 완료 — 회사 PC(6/22):** `.mcp.json`에 이미 등록돼 있던 supabase(원격 http, 프로젝트 범위)가 "인증 필요" 상태였음 → 윤미선이 `/mcp` → supabase → Authenticate로 OAuth 로그인 마침. 이제 `✔ Connected`, supabase 도구(list_tables·execute_sql·get_logs 등) 작동 확인. 연결된 프로젝트 = **msyun8433-hue's Project**(ref `ptluntkhhszuircqztjr`, 서울 ap-northeast-2, ACTIVE_HEALTHY) = 유튜브 대시보드가 쓰는 그 DB. 클로드가 이제 대시보드 DB(daily_snapshots·video_snapshots)를 직접 읽고 다룰 수 있음. ⚠️ 설치(설정)는 USB 따라가지만 **OAuth 인증은 PC마다 한 번씩**(`/mcp`) 다시 필요 — 비밀번호를 USB에 안 담아 더 안전(노션·gws와 같은 원리).

- **회사 PC 세팅: 스킬 재설치 완료 + gws 가져오기 준비(6/22):**
  - **스킬 59개 → 118개로 복구** — 마케팅 45개(`coreyhaines31/marketingskills`: copywriting·cro·ads·pricing·seo-audit 등) + superpowers 13개(`obra/superpowers`: brainstorming·writing-plans·subagent-driven-development 등) git clone 후 `~/.claude/skills/`로 복사. `/brainstorm`·`/properly` 명령 이제 회사 PC에서도 작동. (임시 clone 폴더 `~/_skill_tmp` 남아있음 — 지워도 됨)
  - ⚠️ **humanizer("아임낫에이아이") 미설치** — 종류가 여럿(blader/humanizer 등)이라 어떤 걸 쓰셨는지 윤미선 확인 후 설치 예정. (참고: 고객용 글은 §0·[결다듬기]가 AI티·금지어를 우선 거름)
  - **gws: 회사 PC에선 미설치 + 열쇠 없음 확인** — gws 프로그램 자체가 안 깔렸고 `~/.config/gws/` 열쇠 폴더도 없음(비밀이라 USB 안 넣음, 맥북에만). **맥북에서 가져오기로 함** → 안내 `docs/맥북에서-gws-가져오기.md`(맥북 켜서 "이어가기 읽고 gws 열쇠 가져와" 하면 열쇠+토큰을 USB로 담고 설치법을 적어둠 → 회사 PC에서 복원). gws 설치법이 이어가기에 안 적혀 확인 안 됨 → 맥북에서 캐와야.

- **gws 연결 + 구글 공유문서 읽기 + 윤소정 생각구독 정리(6/22):**
  - **gws(구글 워크스페이스 CLI) 구글 계정 연결 완료** — 클로드가 미선님 지메일·드라이브·캘린더·시트·문서를 직접 다룸. **공유문서함(나와 공유됨)도 읽힘** (claude.ai 웹 커넥터는 '내 것' 위주라 공유문서를 못 읽지만, gws는 드라이브 전체 권한을 받아서 됨). ⚠️ **회사 PC에선 gws 재연결 필요**(설정이 맥북 안에만 있음, USB 안 따라옴 — `gws auth login` 다시. 프로젝트=gws-ieo-021258, 열쇠 `~/.config/gws/client_secret.json`).

- **유튜브 대시보드 완성·배포(6/21):** https://youtube-dashboard-six-omega.vercel.app — 태오의 실행 비즈니스(@teoactbusiness) 구독자·누적조회수·영상수·최근영상·급상승을 매일 08:00 KST 자동수집(Vercel Cron→Supabase). 첫 수집 성공(subs 13100). 코드 `youtube-dashboard/`, 계획·원장 `docs/superpowers/`. 증감은 6/22 두번째 수집부터. 수동수집 명령은 `youtube-dashboard/.env.local`.

- **세션 현황판 v2 Task 3 완료(6/21):** `automations/session-dashboard.mjs`에 renderHtml·main 전면 교체. 오늘 칸·추천·세션별 진도 막대·병목 분포 추가. 표·점검을 `<details>` 접힘으로. CSS STYLE 상수 분리. 발행.md 연동(없으면 발행 0, 있으면 단계 7/7로 판정). 테스트 14/14 통과. 실행 로그: `재료 2 · 대본 6 · 녹음 2 · 발행 0 · 발행률 0%`. 발행.md 추가 후 → `발행 1 · 발행률 10%`. 보고서: `docs/superpowers/plans/task3-report.md`. 다음: UI 브라우저 확인(오늘 칸·추천·진도 막대·병목 막대·접힘 동작).

---


# 이어 거울 (/mirror) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 고객이 보낸 〈하루 끝, 나에게〉 기록을 붙여넣으면, 이어가 세 결의 편지(정돈·비춤·다음 한 걸음)로 비춰 돌려주는 `/mirror` 슬래시 명령 하나를 만든다.

**Architecture:** 코드가 아니라 **프롬프트 명령 파일** 하나(`.claude/commands/mirror.md`)다. 윤미선이 명령을 치고 지메일에서 복사한 기록을 붙여넣으면, AI가 세 결의 편지를 쓰고 → `humanizer`로 AI 말투를 빼고 → `결다듬기`로 금지어를 거른 뒤 → 윤미선에게 보여준다. 발송은 사람이 직접(§10). "테스트"는 자동화가 아니라 **샘플 베타 기록 1개로 한 루프 돌려 6가지 '됐다' 기준을 눈으로 확인**하는 것이다.

**Tech Stack:** Claude Code 슬래시 명령(마크다운 + YAML frontmatter + `$ARGUMENTS`). 보조 스킬: `humanizer`(설치됨), `결다듬기`(프로젝트 스킬).

## Global Constraints

- **거울이지 코치가 아니다** — 정돈·비춤·다음 한 걸음만. 답·조언·해결책·평가·미래 처방·원인 단정·감정 이름 단정 금지. (`자료함/★지금-기준.md` 사업 척추)
- **금지어 0** — 치유·회복·성장·변화·자기혁신·코칭·클래스·교육·명상·소마틱·바디스캔·이완·마음챙김·호흡법, 과장·압박 문구 금지. (CLAUDE.md §5)
- **②비춤의 살짝선** — 반복되는 결 한 가지 + 괄호로 깃털처럼 한 번만 의미 쪽으로. 그 이상 해석 금지. (윤미선 지침 2026-06-23)
- **빈 칸은 지어내지 않는다** — 고객이 안 적은 항목은 건너뛴다. (CLAUDE.md §9 추측·창작 금지)
- **발송 금지** — AI는 글까지만, 메일 발송은 윤미선이 직접. (CLAUDE.md §10)
- **명령 이름은 영어** — `mirror` (한글 /명령은 "/"목록에서 안 먹힘, 이어가기.md 확인).
- **git 아님** — 이 프로젝트는 git 저장소가 아니다. 아래 "Commit" 단계는 모두 **파일 저장 + 확인**으로 읽는다.

---

### Task 1: `/mirror` 명령 파일 작성

**Files:**
- Create: `f:/이어(CAB-STARTER)/.claude/commands/mirror.md`
- 참고(읽기): `하루끝-키트/사이트_레벨2/session.html`(입력 항목 원천), `하루끝-키트/seeds/yunmiseon_seed.md`(voice matching 샘플), `.claude/commands/properly.md`(형식 본보기)

**Interfaces:**
- Produces: 슬래시 명령 `/mirror <붙여넣은 기록>`. 입력 = 고객 기록 텍스트(session.html "내가 적은 것 내려받기" txt 또는 지메일 본문). 출력 = 거울 편지 한 편(윤미선 화면에 표시).
- Consumes: `humanizer` 스킬, `결다듬기` 스킬.

- [ ] **Step 1: 입력 항목 형식 확인**

`하루끝-키트/사이트_레벨2/session.html`의 `asText()` 함수(275~298행)가 만드는 txt 형식을 읽어, 명령이 받을 기록의 실제 모양을 확인한다. 항목 순서: 감각 / 한 단어 / 오래 남은 장면(장면·그때 몸의 감각·장면이 말하는 것) / 목소리 A·B / 오늘의 한 문장·느낌 / 내일의 작은 행동 / 고마웠던 것 / (다음 날) / (베타후기).

- [ ] **Step 2: 명령 파일 작성**

`f:/이어(CAB-STARTER)/.claude/commands/mirror.md`를 아래 내용 그대로 만든다:

````markdown
---
description: (거울) 고객이 보낸 〈하루 끝, 나에게〉 기록을 '이어'가 세 결의 편지로 비춰 돌려준다. 답·조언·평가 없음. (★지금-기준 사업 척추)
---

# /mirror — 이어 거울

윤미선이 이 명령을 쳤다는 것은 **"붙여넣은 고객 기록을, 이어의 거울 편지 한 편으로 비춰라"** 는 직접 지시다.

## 절대 원칙 (자료함/★지금-기준.md 사업 척추)
**거울이지 코치가 아니다.** 정돈·비춤·다음 한 걸음만 준다.
답·조언·해결책·평가·미래 처방·원인 단정·감정 이름 단정은 **주지 않는다.**

## 너(AI)가 할 일 — 이 순서대로

### 1. 기록 읽기
맨 아래 "## 처리할 기록"에 붙은 고객 기록을 읽는다.
항목: 감각/한 단어 · 오래 남은 장면(장면·감각·말) · 두 목소리(A·B) ·
오늘의 한 문장(+느낌) · 내일의 작은 행동 · 고마웠던 것.
**빈 항목은 지어내지 않고 건너뛴다.** (추측·창작 금지)

### 2. 세 결의 편지 쓰기 (한 편의 짧은 편지처럼 — 항목 나열식 금지)
- **① 정돈:** 고객이 쓴 말을 **그의 언어 그대로** 모아 되비춘다. 새 단어로 바꾸지 않는다.
- **② 비춤:** 반복되는 결/실 **한 가지만** 짚는다.
  그다음 괄호로 깃털처럼 **한 번만** 의미 쪽으로 살짝 기운다. 그 이상 해석하지 않는다.
  - ⭕ 예: "'버팀'·'잘 버텼다'·'또 참았네' — 버티고 참는 결이 몇 번 지나가요.
    (무겁다고만 하기엔, 꽤 오래 어깨로 받치고 있었던 것 같기도 하고요.)"
  - ❌ 금지: 원인 단정("~해서예요"), 처방("이렇게 해보세요"), 감정 단정("당신은 외로운 거예요")
- **③ 다음 한 걸음:** 고객이 **이미 적은** '내일의 작은 행동'을 되돌려준다.
  새 과제·조언은 주지 않는다. (안 적었으면 이 문단은 생략)
- **호칭:** '불리고 싶은 이름'이 있으면 "○○님", 없으면 호칭 없이 시작. 끝맺음은 "— 이어".

### 3. AI 말투 빼기
`humanizer` 스킬을 부른다. voice matching 샘플로 `하루끝-키트/seeds/yunmiseon_seed.md`를 쓴다.
같은 문장 구조 반복·과한 접속사("그리고/또한/게다가" 남발)·"~하시면 됩니다" 설명체·
이모지·번호 나열을 없앤다. 윤미선의 담백·정갈한 결로.

### 4. 결 점검
`결다듬기` 스킬을 부른다. 금지어(치유·회복·성장·명상·소마틱·코칭·클래스…)·과장·압박 문구 0을 확인한다.

### 5. 보여주기 (발송 금지)
다듬은 거울 편지 한 편을 윤미선에게 보여준다.
**메일·DM 발송은 하지 않는다** — 윤미선이 읽고 손본 뒤 직접 보낸다. (CLAUDE.md §10)

## 처리할 기록
$ARGUMENTS
````

- [ ] **Step 3: 파일이 명령으로 인식되는지 확인**

Run: `ls "f:/이어(CAB-STARTER)/.claude/commands/mirror.md"`
Expected: 파일 경로가 출력됨 (존재 확인). frontmatter `description:` 한 줄과 `$ARGUMENTS`가 본문에 있는지 눈으로 확인.

- [ ] **Step 4: 저장(=commit 대체)**

git 아님 → 파일이 위 경로에 저장돼 있으면 끝. 별도 커밋 없음.

---

### Task 2: 첫 루프 검증 (샘플 기록 → 거울 편지 → 6기준 확인)

**Files:**
- 읽기: `f:/이어(CAB-STARTER)/.claude/commands/mirror.md`(Task 1 산출물)
- 산출(임시): `docs/superpowers/plans/2026-06-23-이어거울-첫루프결과.md`(검증 기록)

**Interfaces:**
- Consumes: Task 1의 `/mirror` 명령.
- Produces: 첫 루프 검증 기록 1건(6기준 통과 여부 + 실제 생성된 거울 편지).

- [ ] **Step 1: 샘플 베타 기록 준비**

아래 샘플(설계 문서 §3 예시 입력)을 `/mirror`에 넣을 입력으로 쓴다:

```
하루 끝, 나에게 — 민
3. 감각: 무거움, 어깨가 뻐근함
   한 단어: 버팀
4. 오래 남은 장면: 회의에서 내 의견이 묻혔다
   그때 몸의 감각: 가슴이 조임
   장면이 말하는 것: 또 참았네
5. 목소리 A: 나는 지금 괜찮은 척한다
   목소리 B: 하지만 사실 나는 인정받고 싶다
6. 오늘의 한 문장: 오늘의 나는 잘 버텼다
   그 문장의 느낌: 목이 뻐근
7. 내일의 작은 행동: 자기 전 한 문장 적기
오늘 고마웠던 것: 동료가 커피를 사줌
```

- [ ] **Step 2: `/mirror` 실행**

`/mirror` 명령을 치고 위 샘플 기록을 붙여넣는다. 명령이 1~5단계를 밟아 거울 편지를 생성하게 한다.
Expected: 세 결(정돈·비춤·다음 한 걸음)이 한 편의 편지로 흐르고, "민님"으로 시작해 "— 이어"로 끝남.

- [ ] **Step 3: 6가지 "됐다" 기준 체크 (설계 §6)**

생성된 편지를 아래로 점검한다. 하나라도 어긋나면 Task 1의 mirror.md를 고치고 Step 2부터 다시.
- [ ] ① 금지어·과장 0 (치유·회복·성장·명상·코칭… 없음)
- [ ] ② 고객의 언어로 정돈됨 ('버팀'·'또 참았네' 등 원래 단어가 살아 있음, 새 단어로 안 바꿈)
- [ ] ③ 패턴 한 가지만 과하지 않게 짚임 (②비춤의 살짝선 안 — 괄호 깃털 한 번까지만)
- [ ] ④ 답·조언·평가·원인 단정 없음
- [ ] ⑤ AI 말투 없음 (humanizer 통과 — 접속사 남발·설명체·이모지 없음)
- [ ] ⑥ 윤미선이 "이건 나가도 되겠다" 느낌 (← 윤미선이 최종 판단)

- [ ] **Step 4: 검증 기록 저장**

`docs/superpowers/plans/2026-06-23-이어거울-첫루프결과.md`에 ①사용한 샘플 ②생성된 거울 편지 ③6기준 통과표 ④고친 점(있으면)을 적는다.
Run: `ls "f:/이어(CAB-STARTER)/docs/superpowers/plans/2026-06-23-이어거울-첫루프결과.md"`
Expected: 파일 존재.

- [ ] **Step 5: 이어가기.md 갱신**

`이어가기.md` "다음에 할 일"·"지금 어디까지"에 "이어 거울(/mirror) 첫 루프 완료 — 거울 편지 검증됨"을 반영하고, 남은 빈 구멍(메일 자동화·supabase 저장·한 권)은 그대로 "10명 뒤"로 둔다. (CLAUDE.md §8.5 — 조용히 갱신)

---

## Self-Review

**1. Spec coverage:**
- 설계 §1 만들 물건(`/mirror` 명령) → Task 1 ✅
- §2 흐름(붙여넣기→생성→humanizer→결다듬기→보여주기→발송은 사람) → Task 1 Step 2 본문 1~5단계 ✅
- §3 세 결 + 살짝선 + 예시 → Task 1 Step 2 §2, Global Constraints ✅
- §4 안전장치(AI말투·결다듬기·금지동작·빈칸·호칭) → Task 1 Step 2 §3~5 + Global Constraints ✅
- §5 명령 동작 → Task 1 ✅
- §6 6기준 → Task 2 Step 3 ✅
- §7 안 하는 것(메일자동화·supabase·한 권·가지치기) → Global Constraints 발송금지 + Task 2 Step 5(10명 뒤로 둠) ✅

**2. Placeholder scan:** "처리할 기록"의 `$ARGUMENTS`는 Claude Code 명령의 정규 변수(플레이스홀더 아님). 그 외 TBD/TODO 없음. ✅

**3. Type consistency:** 명령 이름 `mirror` 일관. 보조 스킬 이름 `humanizer`·`결다듬기` 일관. 입력 항목 번호(3~7)가 session.html `asText()`와 일치. ✅

# 이 파일을 복사해서 .env 로 이름을 바꾸고, 그 안에만 키를 넣어요.
# .env 는 .gitignore 로 막혀 있어서 깃에 안 올라가요. (키 사고 방지)
# 필요한 것만 채우면 돼요. 안 쓰는 줄은 비워둬도 됩니다.

# 노션 연결 (선택)
NOTION_API_KEY=

# Gmail / 구글 (선택)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# 데이터베이스 (선택 · 대시보드 만들 때)
SUPABASE_URL=
SUPABASE_ANON_KEY=
